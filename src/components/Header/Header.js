import React from "react";

const Header = ({ style }) => {
  return (
    <header style={style}>
      <img width="100px" height="auto" src="/boggle.png" alt="Boggle Logo" />
    </header>
  );
};

export default Header;
